#include "main.h"

/**
 * add -function that add two integers and returns the result
 * @a: int added to b
 * @b: int added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}

